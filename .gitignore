.venv/
__pycache__/
*.pyc
*.pyo
.coverage
htmlcov/
dist/
*.egg-info/
.env
.env.*

# dataset archives (large binaries — keep out of git)
*.zip

# local agent/editor state — never commit personal Claude Code config/session data
.claude/
