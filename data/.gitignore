*.json
*.evtx
*.zip
*.gz
!fetch.sh

# Per-run triage artifacts + logs (generated; regenerated by the sweep scripts)
runs/

# Authoritative ground-truth labels are committed (re-include past the *.json rule)
!ground_truth/apt3_attack_steps.json
